# 音频播放

Windows 版提供共享与独占两类 WASAPI 输出模式。

## 共享模式

Windows 音频引擎统一混合各个应用的声音，因此 Salt Player 播放时，其他应用也可以同时使用同一输出设备。

## 独占模式

独占模式让 Salt Player 单独使用输出设备。启用后，其他应用通常不能通过该设备播放声音；当你需要其他应用同时出声时，请切回共享模式。

独占模式和共享模式的差异取决于 Windows、输出设备和当前音频设置。一般聆听可以先使用共享模式，再根据设备和使用场景调整。
